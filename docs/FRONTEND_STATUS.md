# PFS 前端状态

> 更新时间：2026-08-30
>
> 本文只记录当前工作区已经核验的前端事实，不把静态设计、源码存在或本地启动成功写成完整产品能力。

## 一句话结论

PFS 已经形成独立的视觉方向，并有一个可以本地打开的 Flask 工作台；真实工作台的移动侧栏交互已经完成一组 390×844 的浏览器验收，但当前仍同时存在静态设计预览和真实应用页面两套前端表面，静态页的交互尚未接入，真实工作台的完整数据分析链路和生产级验收也没有完成。

## 当前入口

| 入口 | 当前事实 | 状态 |
| --- | --- | --- |
| /index.html | 独立品牌的静态工作台设计预览；包含侧栏、会话状态、分析空状态、示例问题和输入区 | 已存在；仅视觉预览 |
| / | Flask 渲染 templates/agent_chat.html，加载 static/css/agent_chat.css 和 static/dist/chat-app.js | 已存在；实际应用入口 |
| /dashboard/... | Flask 仪表盘页面入口 | 已存在；需结合真实分析结果继续核验 |

## 前端能力状态

| 能力 | 状态 | 证据与边界 |
| --- | --- | --- |
| PFS 独立品牌、中文产品名和产品图标 | 已实现 | 服务端身份配置位于 config/product_identity.py；真实入口页面标题为 PFS 数据分析 Agent |
| 静态页视觉系统 | 已实现 | index.html 已有深墨绿侧栏、暖色画布、衬线标题、状态标签、示例问题、响应式断点、焦点样式和 reduced-motion 处理 |
| 静态页交互 | 待实现 | index.html 当前没有脚本；导航、示例问题和“开始整理”按钮不会执行真实动作。本轮增加“设计预览”状态提示，避免把该页面误认成生产入口 |
| Flask 工作台框架 | 已实现 | 真实 / 页面已加载 Vue、模块化脚本和 chat-app.js；侧栏包含会话、模型、数据源、MCP、工作目录、Agent 模式、Skills、历史对话等入口 |
| 数据源连接和数据预览 UI | CSV 浏览器上传已本地真实通过；其余需继续验证 | 真实页面已完成选择 data/fixtures/pfs_sales.csv、提交上传并在数据链接列表回读新数据源；XLSX、数据库、复杂 Excel 和完整数据预览仍需逐项验证 |
| Skills / MCP / 模型配置 UI | 已实现但需继续验证 | 真实工作台存在相应面板和接口调用；当前本地启动时没有可用模型或外部服务，不等于真实链路已验收 |
| 报表口径、图表和分析产出展示 | 部分实现 | 固定/上传 CSV/XLSX 报表预览已展示指标、分组、Claim、Evidence 和快照哈希；预览已接入服务端重新计算的 JSON/CSV 下载，Word/Excel/PPT 和复杂图表仍需逐项核验 |
| Evidence / Claim 追踪界面 | 待实现 | 当前已有 PFS 业务层和报表预览相关代码，但完整的证据链阅读、Claim 关联、冲突和待裁决交互尚未作为前端闭环验收 |
| 移动端侧栏交互切片 | 已验证（局部） | 390×844 已验收 Skills、业务知识库、MCP 互斥切换，Escape、Agent 导航关闭，会话文件/数据链接抽屉、backdrop 外侧点击和异步过期请求保护；不代表完整移动端分析工作流完成 |
| 移动端完整分析工作流 | 待实现 | 数据预览、输入、运行中、结果、错误和 JSON/CSV 下载流程仍需在 390px 等视口逐项验收 |
| 生产部署和线上页面 | 未验证 | 本轮只检查本地静态服务和本地 Flask 入口；不能由本地页面推导已提交、已推送、已部署或线上已更新 |

## 本轮已核验

- 静态页可在本地打开，页面标题为 PFS · 报表数据分析工作台。
- 静态页桌面截图检查：1280×720 视口下侧栏、主工作区、示例问题和输入区整体无明显横向溢出。
- 静态页当前为设计预览，脚本数量为 0；按钮是视觉交互稿，不代表已经绑定行为。
- Flask / 可在本地打开，页面标题为 PFS 数据分析 Agent。
- Flask 实际页面已经加载 theme_bootstrap.js、marked、DOMPurify、Vue 和 chat-app.js。
- 本轮在 `PFS_PORT=5012 .venv/bin/python app.py` 本地服务上完成 390×844（390×844 CSS 像素）真实工作台验收：
  - Skills、业务知识库、MCP 面板可以互斥切换，打开面板的 `opacity`、`pointer-events`、`aria-hidden` 和 active nav 状态一致。
  - Escape 会关闭当前面板或抽屉并恢复 Agent 导航；点击 Agent 导航也会关闭所有侧栏 surface。
  - 历史对话打开会话文件抽屉；数据源入口打开数据链接抽屉；点击 backdrop 外侧区域可以关闭抽屉。
  - 模拟约 1.4 秒网络延迟时，Knowledge/Skills 加载完成后的过期请求不会重新打开已取消的面板；切换到 MCP 后最终只保留 MCP。
- 本轮通过 `pnpm run build:chat` 和 `pnpm run build:check`；前者转换 54 个模块并通过 `verify-chat-bundle`，后者通过 Dashboard、Chat 两套 production build。
- PFS 运行期身份已统一：应用加载、事件委托、弹层栈、弹层监听器和侧栏注册锁使用 `__pfs*` 标记，核心事件总线、页面运行时、UI 注册表、团队/工作流、业务画布、聊天重试、自动保存、报表预览、MCP、知识库和工作目录模块使用 `globalThis.PFS`；Dashboard/Chat bundle 已按当前源码重建，Chat 为 54 个模块并通过 bundle 校验。最终仓库和桌面包仍需再做一次交付级审计。
- 远程训练入口已增加 PFS 规范模块，SSH 预检优先调用 PFS runner；当前只有协议和拒绝任意命令的离线测试，未连接真实远程 GPU。
- 本地启动仍记录两个环境提示：`pyodbc: libodbc.2.dylib not found`、`flowchart server: No module named 'MCP.flowchart_server'`。这两个提示未在本轮侧栏交互中复现为失败，但相关数据源/流程图能力仍待环境处理和单独验证。
- 真实工作台的固定 PFS 报表预览已验证服务端重新计算的 JSON 下载反馈；上传 CSV/XLSX 的 JSON/CSV 导出由 HTTP 测试验证，尚未完成浏览器上传后点击下载的文件落盘回读和移动端导出验收。
- 当前工作区已初始化本地 Git 并创建初始提交；两次 push 均因连接 `github.com:443` 超时未成功，远端仍为空，尚未部署。

### 2026-08-30 浏览器 CSV 上传闭环

- 在 PFS_PORT=5012 本地 Flask 服务、Ego Browser 1280×900 桌面视口中，实际选择 data/fixtures/pfs_sales.csv 并点击上传。
- 文件输入回读为 1 个文件，上传按钮由禁用变为可用；提交后弹层关闭，数据链接列表出现 上传数据_pfs_sales.csv_20260830_044644。
- 随后在“口径预览”中选择 pfs_sales.csv · 9 rows，列选择器加载 month / region / product / sales_amount；运行后完成确定性分析并展示销售额合计 100,000、9 行、分组结果、2 条 Claim 和 csv_snapshot Evidence。
- 本证据覆盖浏览器 CSV 上传 → 报表预览选择 → 确定性分析 → Claim/Evidence 展示；不等于浏览器 XLSX、自然语言 Agent、导出或部署已验收。

### 2026-08-30 用户可见兼容标识清理

- 工作区移除确认文案统一称为“工作区元数据”，PFS 工作区指令文件只使用 PFS 文件名。
- 身份迁移、PFS HTTP 垂直切片和远程 runner 身份测试共 36 项通过；`pnpm build` 转换 Dashboard 3 个模块、Chat 54 个模块并通过 bundle 校验。
- 本地启动仍提示缺少 `MCP.flowchart_server`；MCP 真实连接、自然语言 Agent 的复杂意图、导出和部署仍未因此完成验收。
- 新建图表存储在只读部署环境下的临时回退目录使用 `/tmp/pfs`；最终发布前还要清理或隔离开发机上的历史日志和缓存。

## 下一轮建议顺序

1. 在真实工作台补做 XLSX 浏览器上传 → 报表预览 → 结果核对，并在桌面和 390px 视口分别记录结果。
2. 为 JSON/CSV 下载增加浏览器上传后的文件落盘回读；再把 Word/Excel/PPT/复杂图表导出接到统一结果容器。
3. 将导出结果保存为可追踪的工作区 artifact，关联运行 ID、数据快照哈希、Claim/Evidence 和下载历史。
4. 用固定 fixture 补前端冒烟测试，覆盖空状态、加载中、成功、无数据、数据源失败、模型未配置和导出失败。
5. 最后再安排真实部署验收，并分别记录 local、commit、push、deploy、live 状态。

## 明确不应从当前状态推导的结论

- 不能因为页面能打开，就说真实模型任务已经成功。
- 不能因为 Skills、MCP、图表或报表代码存在，就说所有能力已在真实环境可用。
- 不能因为本地 Flask 启动，就说 GitHub 仓库、部署服务或线上页面已经更新。
- 不能把 /index.html 静态设计稿当作当前 Flask / 的生产 UI。
