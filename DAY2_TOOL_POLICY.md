# PFS 第 2 天：Agent Loop 与 Tool Calling 原理

> 学习目标：理解 Agent 如何通过工具完成任务，以及如何用系统策略限制模型，而不是学习某个具体函数的写法。

## 1. Tool Calling 解决什么问题

模型本身只能生成文本。它不知道数据库当前有什么数据，也不能天然读取文件、运行 SQL、绘图或导出报表。

Tool Calling 给模型提供一种结构化的行动方式：

```text
模型：我想调用 query_data，参数是某条 SQL
系统：检查这个工具和参数是否允许
系统：执行 SQL，返回结构化结果
模型：读取结果，决定下一步
```

因此，模型不是直接执行动作，而是提出一个“动作请求”。真正执行动作的是应用系统。

## 2. Agent Loop 的完整闭环

```text
观察当前状态
    ↓
请求模型决定下一步
    ↓
得到普通文本或 Tool Call
    ↓
解析工具名和参数
    ↓
检查工具白名单、参数、权限、预算和风险
    ↓
执行工具
    ↓
记录结果、来源、耗时、成本和错误
    ↓
把结果提供给模型，继续或结束
```

这个循环可以被概括为：

> Model decides, system validates, tool executes, state records.

中文就是：

> 模型决策，系统校验，工具执行，状态记录。

## 3. 四个必须分开的概念

### 3.1 Tool Schema：工具说明书

告诉模型：

- 工具叫什么。
- 什么时候使用。
- 参数有哪些。
- 参数类型和必填项是什么。
- 返回结果大致是什么。
- 有哪些使用限制。

Schema 只是说明书，不代表工具一定安全，也不代表工具一定执行成功。

### 3.2 Tool Registry：工具目录

记录系统实际支持哪些工具，以及工具名如何映射到执行器。

它解决的是：

- 模型请求的工具是否存在。
- 当前任务允许看到哪些工具。
- 工具版本是否正确。
- 工具属于哪个能力域。

### 3.3 Tool Executor：工具执行器

真正完成查询、计算、绘图、导出或外部调用。

执行器必须独立于模型，能够自己校验输入、处理异常、限制资源，并返回统一结果。

### 3.4 Policy Gate：策略门

决定一次工具调用是否允许执行。

检查内容包括：

- 当前用户和工作区是否有权限。
- 工具是否在本轮 allowlist 中。
- 参数是否符合 schema。
- SQL 是否只读、数据范围是否允许。
- 是否超出行数、时间、Token、费用或调用次数限制。
- 是否涉及写入、外发或其他副作用。
- 是否需要人工审批。

## 4. PFS 的工具分级

| 等级 | 典型工具 | 默认策略 |
|---|---|---|
| L0 观察 | 查看 schema、字段、数据质量、指标字典 | 默认允许，范围受限 |
| L1 计算 | SQL 查询、统计分析、图表生成 | 允许，但必须校验输入和资源 |
| L2 产物 | 保存分析结果、导出 Excel/报告 | 记录 artifact，必要时确认 |
| L3 外部访问 | MCP、网页、外部 API、企业数据源 | 单独授权、限域名/数据范围 |
| L4 副作用 | 写数据库、发送报告、修改文件、更新业务系统 | 默认禁止或必须人工审批 |

越接近 L4，越不能让模型自由决定。

## 5. PFS 最小工具契约

每个工具至少要有以下信息：

```text
tool_id              工具唯一名称
purpose              解决什么问题
input_schema         输入参数和类型
output_schema        返回数据结构
allowed_scope        可以访问哪些数据/文件/资源
resource_limits      行数、时间、Token、费用、并发限制
side_effect          是否改变外部状态
approval             是否需要人工确认
idempotency          重试时如何避免重复动作
error_codes          失败类型和用户可理解的提示
audit_fields         运行 ID、参数摘要、来源、耗时、成本
```

## 6. PFS 核心工具如何协作

以“分析各月份销售额并画趋势图”为例：

```text
get_schema
    ↓ 确认真实表名和列名
profile_data
    ↓ 检查日期、缺失、重复和数据范围
query_data
    ↓ 执行受限聚合 SQL
select_chart
    ↓ 根据用户意图和字段角色选择图表
generate_chart
    ↓ 生成图表 artifact
final_answer
    ↓ 输出带口径、计算过程和证据状态的结论
```

这里的顺序很重要：

- 不知道真实字段时不能猜 SQL。
- 不知道数据质量时不能直接解释趋势。
- 不知道图表字段角色时不能直接猜图表参数。
- 没有记录计算过程时，结论无法复核。

## 7. 错误如何在框架中传播

工具失败不应该变成一段模糊的“分析失败”。应按层处理：

```text
参数解析失败       → 不执行工具，要求模型修正
工具不存在         → 拒绝调用，记录 unknown_tool
权限不足           → 拒绝调用，记录 forbidden
资源超限           → 拒绝或排队，记录 budget_exceeded
SQL 不合法         → 不执行，返回可修正错误
数据为空           → 正常结束或请求用户缩小范围
执行超时           → 取消/重试/转人工
外部服务失败       → 按错误类型重试或降级
副作用结果不确定   → 用幂等键查询状态，不直接重复执行
```

错误结果也要成为模型可读取的结构化信息，但错误信息本身不能覆盖系统策略。

## 8. Agent 的四类预算

PFS 至少要控制四类预算：

1. **步骤预算**：最多循环多少次、最多调用多少个工具。
2. **时间预算**：单次运行、单个工具和等待审批的最长时间。
3. **资源预算**：最大行数、结果大小、并发数、内存和文件大小。
4. **费用预算**：Token、模型费用、外部 API 费用和导出成本。

预算由系统预先设定。模型只能看到剩余预算，不能自己提高上限。

## 9. 为什么不能把所有能力都暴露给模型

工具过多会带来四个问题：

- 模型更容易选错工具。
- prompt 和 schema 占用更多上下文。
- 危险工具更容易被误调用。
- 出错后很难判断是模型、参数、工具还是数据的问题。

更好的方式是按当前任务动态暴露工具：

```text
普通数据探索：get_schema / profile_data / query_data
图表任务：加 select_chart / generate_chart
导出任务：加 export，但进入确认门
外部写入任务：默认隐藏，明确授权后才出现
```

## 10. PFS 第 2 天的设计结论

### 结论一

Agent 的智能来自“模型 + 工具 + 反馈循环”，不是单独来自模型。

### 结论二

工具是 Agent 的手脚，Policy Gate 是 Agent 的边界。没有策略门的工具调用不能用于生产。

### 结论三

工具调用的结果必须同时服务三个对象：

- 给模型继续决策。
- 给用户理解过程。
- 给系统进行审计、计费和恢复。

### 结论四

PFS 的第一批核心工具应该从只读、可复现、易验证的工具开始：

```text
get_schema
profile_data
query_data
run_analysis
select_chart
generate_chart
```

写入、外发、更新业务系统等动作放到后续阶段，并默认需要审批。

## 11. 与当前原项目的对应关系

原项目可以作为这些原理的案例：

- `agent/tools/schemas.py`：Tool Schema。
- `agent/agent.py`：Agent Loop、工具分派、错误和事件。
- `agent/validate.py`：参数与范围校验。
- `agent/tools/business/data.py`：数据分析工具执行器。
- `agent/tools/results.py`：工具结果封装。
- `agent/events.py`：运行事件。

这些文件证明“原项目设计了这些机制”，但不代表所有真实运行路径已经通过验收。PFS 后续会保留有效原理，并重新定义自己的工具契约、命名、策略和证据模型。

## 12. 第 2 天的改造边界

今天只完成框架设计，不直接重写所有工具：

- 明确工具分级。
- 明确工具契约字段。
- 明确 Agent Loop 的停止条件。
- 明确预算、权限、审批和错误边界。
- 为下一阶段的 mock provider 和第一个只读工具做好设计准备。

第 3 天将进入 Context Engineering、Memory、Knowledge 和 Artifact-first 原理。

## 13. 本轮实现快照

已在项目根目录新增 `pfs_agent/` 基础层：

- `contracts.py`：工具风险、审批方式、`ToolSpec` 和 `ToolCall`。
- `policy.py`：`ToolRegistry`、`PolicyContext`、`PolicyDecision` 和 `PolicyGate`。
- `tests/test_pfs_tool_policy.py`：6 个单元测试。

本轮验证结果：6 tests passed，且 Python 3.9 语法编译通过。该模块尚未连接原项目的 `BusinessAgent`、真实模型或真实工具执行器，因此只应标记为“基础契约已实现、主链路待集成”。
